<%= form_with model: @fantastic_book, local: true do |form| %>
 
  <% if @fantastic_book.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@fantastic_book.errors.count, "error") %> prohibited
        this fantastic_book from being saved:
      </h2>
      <ul>
        <% @fantastic_book.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>
 
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>